# 🕵️‍♂️ ⚠️ 免责声明**：本项目仅供网络安全研究与 API 自动化架构测试使用。请遵守相关法律法规及目标网站的服务条款 (ToS)，切勿用于非法获取公民个人隐私信息。



本项目是一个全自动的社交图谱提取与渲染引擎。以真实的**华盛顿 202 区号**为诱饵，结合本地 `curl_cffi` 指纹伪装与 **Apify 云端 API** 的算力，降维打击 Cloudflare 与 hCaptcha 风控。
提取出的“1换多”关联网络数据，将通过 Webhook 自动回流至本仓库的 `projects.json`，并由 GitHub Pages 实时渲染为可视化图谱。


## 🐒 猴子视觉脚本（houzi）

- `houzi_web.py`：截指定网页，喂 qwen-vl 看排版。
- `houzi_local.py`：读同目录第一张 png，喂 qwen-vl 描述。
- 密钥：从环境变量 `DASHSCOPE_KEY` 读，代码里只有占位符 `YOUR_KEY`，**禁止硬编码真密钥**。
- 模型：`qwen3-vl-32b-thinking`，调用时 `enable_thinking=False` 关内耗。
- 运行：`set DASHSCOPE_KEY=真密钥 && python houzi_web.py`
- **不要改动** fetcher.py / lumache.py / 出口IP.md 等原有文件，猴子与它们无关、互不依赖。



## 🏗️ 核心架构工作流

1. **诱饵探测 (Local)**：使用 Python `curl_cffi` 模拟真实 Chrome TLS 指纹，向 TPS 发送华盛顿 202 区号请求。
2. **云端接管 (Apify API)**：一旦触发 hCaptcha 或 IP 封锁，本地脚本立即调用 Apify API，启动云端打码 Actor 或住宅代理池。
3. **数据回流 (Webhook)**：Apify 爬虫跑完后，通过 Webhook 将清洗好的 JSON 数据直接 Push 到本仓库。
4. **自动部署 (GitHub Actions)**：触发 `pages.yaml` 工作流，更新 `projects.json`，前端 `index.html` 无缝刷新。

---

## ⚙️ Apify API 核心调用指南 (实战代码)

*基于 Apify OpenAPI v2 规范提炼，拒绝废话，直接上接口。*

### 1. 启动云端打码/爬虫 Actor (异步)
当本地遭遇 hCaptcha 拦截时，调用此接口将任务甩给云端。
你的代码
   │
   ▼
asyncio.run(main()) ──→ 所有 Nodriver 操作必须 await
   │
   ▼
nd.start(headless=False) ──→ 必须 GUI 模式才能过 Turnstile
   │
   ▼
Linux? ──→ xvfb-run -a ──→ 虚拟显示器骗过 Chrome
   │                           │
   ▼                           ▼
浏览器以为自己有屏幕 ◄────── 分辨率要和 User-Agent 一致
   │
   ▼
Cloudflare Turnstile ──→ 看到正常插件/屏幕/鼠标轨迹 ──→ 放行
